server:
  port: 8762
spring:
  application:
    name: nativeconfigserver
#    配置本地获取文件的方式
  profiles:
    active: native
#    配置本地存放文件的路径
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared